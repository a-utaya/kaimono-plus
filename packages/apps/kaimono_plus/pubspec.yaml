name: kaimono_plus
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.3

resolution: workspace

dependencies:
  auth:
    path: ../../core/auth
  flutter:
    sdk: flutter
  gap: ^3.0.1
  provider: ^6.1.1

dev_dependencies:
  altive_lints: ^2.1.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
