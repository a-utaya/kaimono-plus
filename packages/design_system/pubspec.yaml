name: design_system
description: デザインシステムパッケージ（テーマ、カラー、タイポグラフィなど）
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.10.3

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
